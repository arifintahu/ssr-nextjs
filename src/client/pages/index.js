import { Button } from 'antd';

export default function Home() {
  return (
    <div>
        <Button type="primary" style={{ marginLeft: 8 }}>
          Primary Button
        </Button>
    </div>
  )
}
